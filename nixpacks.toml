[phases.setup]
nixPkgs = ["php82", "php82Extensions.pdo", "php82Extensions.pdo_mysql", "php82Extensions.mbstring", "php82Extensions.openssl", "php82Extensions.curl"]

[phases.install]
cmds = ["echo 'no composer install needed'"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t backend backend/router.php"
